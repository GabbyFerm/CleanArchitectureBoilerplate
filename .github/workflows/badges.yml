name: Update Badges

on:
  push:
    branches: [ main ]
  workflow_run:
    workflows: [".NET CI"]
    types: [ completed ]

jobs:
  update-badges:
    runs-on: ubuntu-latest
    
    steps:
    - name: Create badge JSON
      run: |
        echo "Badge data updated"
        # Badges are auto-generated by shields.io based on workflow status
